# MatList — คู่มือสำหรับ Claude Code

อ่านไฟล์นี้ก่อนเริ่มทำงานทุกครั้ง ไฟล์นี้คือแหล่งความจริงเรื่อง"ทำอะไร ทำไม และอะไรห้ามแตะ"

## MatList คืออะไร
เครื่องมือทำตารางสเปกวัสดุสำหรับสถาปนิกไทย ที่ต่อท่อส่งใบขอราคา (RFQ) ไปหาผู้ขายวัสดุในตัว
- **กระดูกสันหลังคือ "โครงการ + ตารางสเปก"** ไม่ใช่ catalog — เครื่องมือต้องมีค่าตั้งแต่ยังไม่มีผู้ขายสักราย
- **รายได้หลักมาจากฝั่งผู้ขาย** (จ่ายเพื่อรับ lead + เครื่องมือตอบ) ฝั่งสถาปนิกเป็น freemium (จ่ายเมื่อใช้ VE Finder / Revit)
- ผู้ใช้ 2 ฝั่ง: **ผู้ออกแบบ** (Designer) และ **ผู้ขาย** (Seller) มี workspace แยกกัน

## กติกาเหล็ก (ห้ามละเมิดเด็ดขาด ไม่ว่าจะสะดวกแค่ไหน)
1. **ความเป็นกลาง**: ผลค้นหาเรียงตามความตรงสเปกเท่านั้น — ห้ามมีการซื้ออันดับ, Boost ในหน้าเทียบ/ผลค้นหา, หรือโฆษณาแฝง ความเป็นกลางต่อสถาปนิกคือสินค้าที่เราขายให้ผู้ขาย พังฝั่งเดียวพังทั้งระบบ
2. **ราคา lead เดียวทุกเจ้า** — ไม่มีจ่ายเพิ่มเพื่อขึ้นก่อน
3. **ข้อมูลขายเฉพาะ aggregate** — ห้ามขายข้อมูลรายออฟฟิศ/รายโปรเจกต์เด็ดขาด
4. **ความเป็นส่วนตัวของ RFQ**: ผู้ขายเห็นบริบทสเปก แต่ไม่เห็นเบอร์ผู้ออกแบบจนกว่าผู้ออกแบบจะตอบกลับ
5. **PDPA**: การส่ง RFQ หา contact ที่ยังไม่สมัคร ต้องผ่านการตรวจทางกฎหมายก่อนเปิดใช้ — อย่า implement การ scrape/ส่งหา contact สาธารณะโดยไม่มี flag ให้มนุษย์ตรวจ

## สถานะปัจจุบัน
- มี prototype ที่ `reference/matlist-prototype.jsx` — เป็น React หน้าเดียว state อยู่ใน memory ทั้งหมด (รีเฟรชแล้วหาย) ไม่มี backend
- prototype นี้คือ **แหล่งอ้างอิง UX/flow ที่ผ่านการตกลงแล้ว** ให้ยึดเป็นต้นแบบหน้าตาและพฤติกรรม แต่ต้องรื้อเป็นสถาปัตยกรรมจริงตาม `docs/ARCHITECTURE.md`

## ลำดับงาน (อ่าน docs/ROADMAP.md ก่อนหยิบ task)
งานถูกแบ่งเป็น phase ใน `docs/ROADMAP.md` — **ทำเรียงตาม phase ห้ามข้าม** เพราะ phase หลังพึ่ง phase หน้า
เริ่มที่ Phase 1 task แรกเสมอ ถ้าไม่แน่ใจว่าทำอะไรต่อ ให้ถามผู้ใช้ อย่าเดา

## เอกสารในชุดนี้
- `docs/ARCHITECTURE.md` — stack, โครงสร้างโปรเจกต์, การตัดสินใจเชิงเทคนิค
- `docs/DATA_MODEL.md` — schema ฐานข้อมูลทั้งหมด (อ่านก่อนเขียน migration ใด ๆ)
- `docs/ROADMAP.md` — task ทั้งหมดแยก phase พร้อม acceptance criteria
- `docs/API_SPEC.md` — endpoint หลักที่ต้องมี
- `docs/DECISIONS.md` — เหตุผลเบื้องหลังการตัดสินใจ (อ่านเพื่อไม่ทำผิดซ้ำ)
- `docs/DEPLOY.md` / `docs/OPERATIONS.md` — ขึ้น production + ดูแลระบบ
- `docs/legal/` — ร่าง Privacy/Terms + PDPA checklist (รอทนายตรวจ)
- `reference/matlist-prototype.jsx` — prototype อ้างอิง

## หลักการทำงาน
- **ทำทีละ task ให้จบและ test ผ่านก่อนขึ้น task ถัดไป** — commit ย่อย ๆ
- ทุก task มี acceptance criteria ใน ROADMAP — ต้องผ่านครบก่อนถือว่าเสร็จ
- เจอทางแยกที่กระทบกติกาเหล็กหรือ schema หลัก → หยุดถามผู้ใช้ ไม่ตัดสินเอง
- เขียน test สำหรับ business logic (การคิดสถานะ RFQ, การจับคู่สเปก, การคิดสิทธิ์ผู้ใช้) เสมอ
- ภาษาใน UI ต้องมีทั้ง TH/EN (default TH) — ทุก string ผ่านระบบ i18n ห้าม hardcode
